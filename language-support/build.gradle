plugins {
    id 'java-library'
}

dependencies {
    api project(':core')

    testImplementation 'org.assertj:assertj-core'
}

tasks.withType(Test).configureEach {
    jvmArgs '--illegal-final-field-mutation=deny'
}